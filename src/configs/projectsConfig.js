/**
 * Array of objects to be parsed into projects section
 *
 * @NecessaryFields title, route (unique), tagline
 * @OptionalFields icon, Component, sourceCode, projectUrl
 *
 * @Fields
 * - `title` : ProjectCard Title
 * - `icon`: ProjectCard Icon / Display Image
 * - `tagline`: Short description of a project, in a sentence
 * - `route` : URL to project page on this website
 * - `sourceCode`: URL to GitHub repository
 * - `projectUrl`: URL to current hosted project
 * - `Component` : Component to be displayed when project view is expanded to show the project page
 */
const projects = [
    {},
];